## What does this PR do?

<!-- Describe the behavior change, not just the code. Link the issue: Fixes #NNN -->

## Checklist

- [ ] Tests added/updated for behavior changes (`pytest -q` is green locally)
- [ ] Invariants in [docs/architecture.md](docs/architecture.md) still hold — or the doc is updated with the change
- [ ] No new heavyweight dependencies (no DB, no queue, no framework sprawl)
- [ ] No secrets, key values, or real endpoints committed
- [ ] CHANGELOG.md updated (for user-visible changes)

## How was this tested?

<!-- e.g. unit tests, manual curl against /v1/chat/completions, rebuilt the .deb -->
